# SGC
# Prueba de contador de números

Proyecto de prueba técnica con:

- Frontend de Angular
- Backend de API web .NET
- Base de datos de SQL Server

## Estructura

- `/backend/NumberManagementAPI` → API .NET
- `/frontend/number-ui` → Aplicación Angular

## Cómo ejecutar

### Backend
Abrir `backend/NumberManagementAPI` en Visual Studio y ejecutar la API.

### Frontend
Abrir `frontend/number-ui` y ejecutar:

### Base de datos
Ejecutar la query que se encuentra en la raiz del proyecto, esta crea la BD su tabla en memoria
